services:
  oidc-bridge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: oidc-bridge
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
